---
import { getReadingMinutes } from '@/lib/posts';
import type { Lang } from '@/i18n/ui';

interface Props {
  text: string;
  lang: Lang;
}

const { text, lang } = Astro.props;
const minutes = getReadingMinutes(text);
---

<span>{lang === 'zh' ? `${minutes} 分钟` : `${minutes} min`}</span>
