<section class="departure-story" aria-label="blog的出发">
  <div class="chalk-sky" aria-hidden="true">
    <span class="spark spark-a"></span>
    <span class="spark spark-b"></span>
    <span class="spark spark-c"></span>
  </div>

  <section class="chalk-board hero-card">
    <p class="tiny-label">strawberry chalk diary</p>
    <h2>把第一行字，写成一颗粉色的出发糖</h2>
    <p>
      这个 blog 不想一开始就装作很成熟。它可以有一点点害羞，有一点点闪光，
      像放学后还没有擦掉的粉笔字，轻轻落在一块草莓牛奶色的黑板上。
    </p>
    <div class="chalk-tray" aria-hidden="true">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
  </section>

  <div class="sticker-grid">
    <article class="sticker-card card-pink">
      <span class="pin"></span>
      <p class="card-kicker">小小宣言</p>
      <h3>写得可爱，也写得认真</h3>
      <p>
        少女感不是逃避现实，而是把现实里粗糙的边缘，用柔软的颜色重新描一遍。
        我想在这里记录生活、审美、失误、心情，还有那些突然亮起来的小灵感。
      </p>
    </article>

    <article class="sticker-card card-mint">
      <span class="pin"></span>
      <p class="card-kicker">出发清单</p>
      <ul>
        <li>允许文章带着手写的呼吸感。</li>
        <li>允许页面像粉笔末一样有一点颗粒。</li>
        <li>允许严肃想法穿一条粉色裙子出场。</li>
        <li>允许慢慢来，但不要停在原地。</li>
      </ul>
    </article>

    <article class="sticker-card card-blue">
      <span class="pin"></span>
      <p class="card-kicker">给未来的页面</p>
      <h3>请保持一点天真</h3>
      <p>
        等这个 blog 长大以后，也希望它不要只剩下整齐的目录。最好还留着几条歪歪扭扭的线，
        留着第一次出发时的粉笔味道。
      </p>
    </article>
  </div>

  <section class="letter-card">
    <div class="letter-ribbon" aria-hidden="true"></div>
    <p class="tiny-label">dear blog</p>
    <h2>今天从这里开始</h2>
    <p>
      我把这一页做得像一块被认真装饰过的小黑板：有粉色，有薄荷绿，有云朵一样的蓝，
      还有不太规矩的线条。它不是为了显得完美，而是为了告诉自己：写作可以很轻，也可以很坚定。
    </p>
    <p>
      所以，blog 的出发不是一个盛大的仪式。它更像把一支短短的粉笔握在手里，
      在心里说一句：“好呀，那就从今天开始吧。”
    </p>
  </section>
</section>

<style>
  .departure-story {
    position: relative;
    isolation: isolate;
    display: grid;
    gap: 28px;
    padding: 4px 0 24px;
    color: #4e3458;
    font-family: 'Gaegu', 'Noto Serif SC', sans-serif;
    letter-spacing: 0;
  }

  .departure-story::before {
    position: absolute;
    inset: -40px -22px auto;
    z-index: -2;
    height: calc(100% + 80px);
    border-radius: 42px;
    background:
      radial-gradient(circle at 16% 12%, rgba(255, 255, 255, 0.82) 0 2px, transparent 3px 100%),
      radial-gradient(circle at 84% 18%, rgba(255, 119, 183, 0.18) 0 68px, transparent 69px),
      radial-gradient(circle at 18% 78%, rgba(157, 235, 207, 0.22) 0 84px, transparent 85px),
      linear-gradient(135deg, #fff7fc 0%, #ffe2f0 48%, #f4eeff 100%);
    content: '';
  }

  .chalk-sky {
    position: absolute;
    inset: -22px 0 auto;
    z-index: -1;
    height: 160px;
    pointer-events: none;
  }

  .spark {
    position: absolute;
    width: 38px;
    height: 38px;
    opacity: 0.78;
  }

  .spark::before,
  .spark::after {
    position: absolute;
    inset: 16px 0 auto;
    height: 4px;
    border-radius: 999px;
    background: #fff;
    box-shadow: 0 0 0 2px rgba(255, 119, 183, 0.2);
    content: '';
  }

  .spark::after {
    transform: rotate(90deg);
  }

  .spark-a {
    top: 12px;
    right: 9%;
    transform: rotate(12deg);
  }

  .spark-b {
    top: 82px;
    left: 6%;
    transform: rotate(-18deg) scale(0.75);
  }

  .spark-c {
    top: 48px;
    right: 31%;
    transform: rotate(34deg) scale(0.58);
  }

  .chalk-board,
  .sticker-card,
  .letter-card {
    position: relative;
    border: 3px solid rgba(255, 255, 255, 0.9);
    box-shadow:
      0 10px 0 rgba(255, 119, 183, 0.16),
      0 26px 58px rgba(111, 57, 94, 0.14);
  }

  .hero-card {
    overflow: hidden;
    padding: clamp(28px, 5vw, 54px);
    border-radius: 34px 24px 40px 22px / 24px 36px 22px 34px;
    background:
      radial-gradient(circle at 11% 17%, rgba(255, 255, 255, 0.74) 0 2px, transparent 3px 100%),
      radial-gradient(circle at 93% 22%, rgba(167, 221, 255, 0.35) 0 86px, transparent 87px),
      linear-gradient(135deg, #ff9dca 0%, #ffc7df 42%, #cbbcff 100%);
  }

  .hero-card::before,
  .hero-card::after {
    position: absolute;
    pointer-events: none;
    content: '';
  }

  .hero-card::before {
    right: 30px;
    bottom: 32px;
    width: 160px;
    height: 88px;
    border: 4px dashed rgba(255, 255, 255, 0.84);
    border-radius: 50%;
    transform: rotate(-13deg);
  }

  .hero-card::after {
    top: 30px;
    right: 54px;
    width: 92px;
    height: 42px;
    border-top: 5px solid rgba(255, 255, 255, 0.88);
    border-bottom: 5px solid rgba(255, 255, 255, 0.88);
    border-radius: 999px;
    transform: rotate(12deg);
  }

  .tiny-label,
  .card-kicker {
    width: fit-content;
    margin: 0 0 12px;
    padding: 4px 12px;
    border: 2px dashed rgba(78, 52, 88, 0.26);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.62);
    color: #80516f;
    font-size: 17px;
    font-weight: 700;
    line-height: 1.1;
  }

  .departure-story h2,
  .departure-story h3 {
    margin: 0;
    color: #4e3458;
    font-family: 'Gaegu', 'ZCOOL KuaiLe', 'Noto Serif SC', sans-serif;
    font-weight: 700;
    letter-spacing: 0;
  }

  .hero-card h2 {
    max-width: 740px;
    color: #fff;
    font-size: clamp(42px, 7vw, 78px);
    line-height: 0.98;
    text-shadow:
      2px 2px 0 rgba(78, 52, 88, 0.25),
      0 0 16px rgba(255, 255, 255, 0.72);
  }

  .hero-card p:not(.tiny-label) {
    max-width: 710px;
    margin: 22px 0 0;
    color: #5f3d67;
    font-size: clamp(23px, 3vw, 30px);
    font-weight: 700;
    line-height: 1.55;
  }

  .chalk-tray {
    display: flex;
    gap: 12px;
    margin-top: 34px;
  }

  .chalk-tray span {
    width: 68px;
    height: 12px;
    border-radius: 999px;
    background: #fff;
    box-shadow: inset -8px 0 0 rgba(78, 52, 88, 0.1);
    transform: rotate(-2deg);
  }

  .chalk-tray span:nth-child(2) {
    background: #b8f4dc;
    transform: rotate(1deg);
  }

  .chalk-tray span:nth-child(3) {
    background: #b9e5ff;
    transform: rotate(-4deg);
  }

  .chalk-tray span:nth-child(4) {
    background: #f6ddff;
    transform: rotate(3deg);
  }

  .sticker-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }

  .sticker-card {
    min-height: 290px;
    padding: 26px 24px 24px;
    border-radius: 28px 18px 32px 20px / 20px 32px 18px 30px;
    transform: rotate(-1.2deg);
  }

  .sticker-card:nth-child(2) {
    transform: translateY(18px) rotate(1.3deg);
  }

  .sticker-card:nth-child(3) {
    transform: rotate(-0.4deg);
  }

  .card-pink {
    background:
      repeating-linear-gradient(0deg, transparent 0 31px, rgba(255, 255, 255, 0.42) 32px 34px),
      linear-gradient(135deg, #ffe4f2, #ffd0e8);
  }

  .card-mint {
    background:
      repeating-linear-gradient(0deg, transparent 0 31px, rgba(255, 255, 255, 0.44) 32px 34px),
      linear-gradient(135deg, #d9fff1, #ecf7ff);
  }

  .card-blue {
    background:
      repeating-linear-gradient(0deg, transparent 0 31px, rgba(255, 255, 255, 0.44) 32px 34px),
      linear-gradient(135deg, #dff2ff, #f0e8ff);
  }

  .pin {
    position: absolute;
    top: -13px;
    left: 28px;
    width: 72px;
    height: 26px;
    border: 2px solid rgba(78, 52, 88, 0.16);
    border-radius: 12px 8px 13px 8px;
    background:
      repeating-linear-gradient(92deg, rgba(255, 255, 255, 0.88) 0 12px, rgba(255, 119, 183, 0.24) 13px 22px);
    box-shadow: 0 4px 0 rgba(255, 119, 183, 0.14);
    transform: rotate(-5deg);
  }

  .sticker-card h3 {
    margin-bottom: 12px;
    font-size: 34px;
    line-height: 1.05;
  }

  .sticker-card p,
  .sticker-card li,
  .letter-card p {
    color: #5f3d67;
    font-size: 22px;
    font-weight: 700;
    line-height: 1.62;
  }

  .sticker-card p {
    margin: 0;
  }

  .sticker-card ul {
    display: grid;
    gap: 10px;
    margin: 4px 0 0;
    padding: 0;
    list-style: none;
  }

  .sticker-card li {
    position: relative;
    padding-left: 26px;
  }

  .sticker-card li::before {
    position: absolute;
    left: 0;
    top: 0.12em;
    color: #ff77b7;
    content: '♡';
    font-size: 22px;
    line-height: 1;
  }

  .letter-card {
    overflow: hidden;
    padding: clamp(28px, 5vw, 50px);
    border-radius: 28px 20px 34px 18px / 20px 32px 18px 30px;
    background:
      radial-gradient(circle at 86% 18%, rgba(255, 119, 183, 0.22) 0 72px, transparent 73px),
      linear-gradient(135deg, #fff9fd 0%, #ffe7f3 48%, #ecf9ff 100%);
  }

  .letter-card::after {
    position: absolute;
    right: -34px;
    bottom: 18px;
    width: 260px;
    height: 126px;
    border: 4px dashed rgba(255, 119, 183, 0.42);
    border-radius: 55% 45% 48% 52% / 42% 54% 46% 58%;
    content: '';
    transform: rotate(-12deg);
  }

  .letter-ribbon {
    position: absolute;
    top: 22px;
    right: 28px;
    width: 118px;
    height: 42px;
    border-radius: 18px 12px 20px 10px;
    background:
      linear-gradient(135deg, #ff77b7 0 48%, #fff 48% 52%, #c9b7ff 52% 100%);
    box-shadow: 0 7px 0 rgba(255, 119, 183, 0.18);
    transform: rotate(7deg);
  }

  .letter-card h2 {
    color: #ff65aa;
    font-size: clamp(40px, 6vw, 66px);
    line-height: 1;
    text-shadow: 2px 2px 0 #fff;
  }

  .letter-card p {
    max-width: 790px;
    margin: 18px 0 0;
  }

  @media (max-width: 860px) {
    .sticker-grid {
      grid-template-columns: 1fr;
    }

    .sticker-card,
    .sticker-card:nth-child(2),
    .sticker-card:nth-child(3) {
      min-height: auto;
      transform: none;
    }
  }

  @media (max-width: 560px) {
    .departure-story::before {
      inset-inline: -10px;
      border-radius: 28px;
    }

    .hero-card,
    .letter-card,
    .sticker-card {
      padding: 22px 18px;
    }

    .hero-card h2 {
      font-size: 40px;
    }

    .hero-card p:not(.tiny-label),
    .sticker-card p,
    .sticker-card li,
    .letter-card p {
      font-size: 19px;
    }

    .chalk-tray span {
      width: 46px;
    }
  }
</style>
