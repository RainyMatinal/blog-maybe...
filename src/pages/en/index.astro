---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PostCard from '@/components/PostCard.astro';
import { getPosts } from '@/lib/posts';
import { localizedPath } from '@/i18n/utils';

const lang = 'en';
const posts = (await getPosts(lang)).slice(0, 3);
---

<BaseLayout lang={lang} title="Life & Design Notes" description="A quiet bilingual blog about life aesthetics, visual design, travel, and considered objects." switchTo="/">
  <section class="site-shell hero" data-hero>
    <p class="ui-font hero-line">Living with softer attention,</p>
    <p class="ui-font hero-line">designing with fewer noises.</p>
    <p class="en-serif hero-subtitle">Notes on life, visual culture, travel, and the objects that stay with us.</p>
  </section>

  <section class="site-shell section-space latest">
    <div class="section-head">
      <h2 class="ui-font">Recent Writing <span>近期写作</span></h2>
      <a class="ui-font" href={localizedPath(lang, '/blog')}>View all</a>
    </div>
    <div class="post-grid">
      {posts.map((post) => <PostCard post={post} />)}
    </div>
  </section>

  <section class="site-shell about-line">
    <p class="ui-font">A small archive for visual order, gentle pace, and the useful beauty of everyday things.</p>
  </section>

  <script>
    const heroItems = document.querySelectorAll<HTMLElement>('[data-hero] > *');

    heroItems.forEach((element, index) => {
      element.animate(
        [
          { opacity: 0, transform: 'translateY(20px)' },
          { opacity: 1, transform: 'translateY(0)' }
        ],
        {
          delay: index * 100,
          duration: 700,
          easing: 'ease-out',
          fill: 'both'
        }
      );
    });

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;

          entry.target.animate(
            [
              { opacity: 0, transform: 'translateY(18px)' },
              { opacity: 1, transform: 'translateY(0)' }
            ],
            {
              duration: 550,
              easing: 'ease-out',
              fill: 'both'
            }
          );
          observer.unobserve(entry.target);
        });
      },
      { rootMargin: '0px 0px -120px 0px' }
    );

    document.querySelectorAll<HTMLElement>('[data-reveal]').forEach((element) => observer.observe(element));
  </script>
</BaseLayout>

<style>
  .hero {
    position: relative;
    padding: 112px 0 76px;
  }

  .hero::before,
  .hero::after {
    position: absolute;
    z-index: -1;
    border: var(--border-doodle);
    content: '';
  }

  .hero::before {
    top: 78px;
    right: 8%;
    width: 132px;
    height: 132px;
    border-radius: 45% 55% 48% 52%;
    background: var(--color-accent-2);
    box-shadow: var(--shadow-doodle);
    transform: rotate(10deg);
  }

  .hero::after {
    bottom: 18px;
    left: 8%;
    width: 190px;
    height: 46px;
    border-radius: 55% 45% 52% 48% / 46% 54% 42% 58%;
    background: var(--color-surface-strong);
    box-shadow: var(--shadow-doodle-small);
    transform: rotate(-4deg);
  }

  .hero-line {
    margin: 0;
    max-width: 940px;
    font-family: 'Gaegu', 'DM Sans', sans-serif;
    font-size: 82px;
    font-weight: 700;
    line-height: 0.98;
    letter-spacing: 0;
    text-shadow: 4px 4px 0 var(--color-surface-strong);
    transform: rotate(-1deg);
  }

  .hero-line:nth-child(2) {
    color: var(--color-accent);
    text-shadow: 4px 4px 0 var(--color-shadow);
    transform: rotate(1deg);
  }

  .hero-subtitle {
    width: fit-content;
    margin: 32px 0 0;
    padding: 4px 14px;
    border: var(--border-doodle);
    border-radius: 22px 14px 26px 16px / 14px 24px 13px 20px;
    background: var(--color-surface);
    box-shadow: var(--shadow-doodle-small);
    color: var(--color-text);
    font-size: 24px;
    line-height: 1.6;
    letter-spacing: 0;
    transform: rotate(-1deg);
  }

  .section-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 36px;
  }

  .section-head h2 {
    margin: 0;
    width: fit-content;
    padding: 2px 12px;
    border: var(--border-doodle);
    border-radius: 26px 16px 30px 18px / 16px 28px 14px 24px;
    background: var(--color-surface-strong);
    box-shadow: var(--shadow-doodle-small);
    color: #171411;
    font-size: 32px;
    font-weight: 700;
    letter-spacing: 0;
    transform: rotate(-1deg);
  }

  .section-head span {
    margin-left: 8px;
    color: var(--color-accent-2);
    font-family: 'Noto Serif SC', serif;
    font-size: 18px;
    font-weight: 400;
  }

  .section-head a {
    color: var(--color-text);
    font-size: 20px;
    font-weight: 700;
    text-decoration: underline wavy var(--color-accent);
    text-decoration-thickness: 2px;
    text-underline-offset: 0.22em;
  }

  .post-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 28px;
  }

  .about-line {
    width: min(100% - 32px, 760px);
    padding: 18px 24px;
    border: var(--border-doodle);
    border-radius: 30px 18px 34px 16px / 18px 32px 15px 26px;
    background: var(--color-surface);
    box-shadow: var(--shadow-doodle);
    text-align: center;
    color: var(--color-text);
    font-size: 22px;
    font-weight: 700;
    transform: rotate(1deg);
  }

  @media (max-width: 900px) {
    .post-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 80px;
    }

    .hero-line {
      font-size: 48px;
    }

    .post-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
