---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PostList from '@/components/PostList.astro';
import { getPosts } from '@/lib/posts';

const lang = 'en';
const posts = await getPosts(lang);
---

<BaseLayout lang={lang} title="Blog" description="Essays on life, design, travel, and thoughtful products." switchTo="/blog">
  <section class="site-shell page-head">
    <p class="ui-font eyebrow">{posts.length} posts</p>
    <h1 class="ui-font">Blog</h1>
  </section>
  <section class="site-shell section-space">
    <PostList posts={posts} lang={lang} />
  </section>
</BaseLayout>

<style>
  .page-head {
    position: relative;
    padding-top: 88px;
  }

  .page-head::after {
    position: absolute;
    right: 10%;
    bottom: -22px;
    width: 150px;
    height: 34px;
    border: var(--border-doodle);
    border-radius: 50%;
    background: var(--color-surface-strong);
    box-shadow: var(--shadow-doodle-small);
    content: '';
    transform: rotate(-6deg);
  }

  .eyebrow {
    width: fit-content;
    margin: 0 0 12px;
    padding: 2px 12px;
    border: 2px solid var(--color-border);
    border-radius: 18px 12px 20px 13px / 12px 18px 10px 16px;
    background: var(--color-accent-2);
    box-shadow: var(--shadow-doodle-small);
    color: #fffdf3;
    font-size: 18px;
    font-weight: 700;
    transform: rotate(-2deg);
  }

  h1 {
    margin: 0;
    font-family: 'Gaegu', 'DM Sans', sans-serif;
    font-size: 78px;
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: 0;
    text-shadow: 4px 4px 0 var(--color-surface-strong);
  }

  @media (max-width: 640px) {
    h1 {
      font-size: 48px;
    }
  }
</style>
