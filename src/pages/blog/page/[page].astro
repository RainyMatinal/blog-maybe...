---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PostList from '@/components/PostList.astro';
import { getPosts, type BlogPost } from '@/lib/posts';
import type { GetStaticPaths, Page } from 'astro';

export const getStaticPaths = (async ({ paginate }) => {
  const posts = await getPosts('zh');
  return paginate(posts, { pageSize: 10 });
}) satisfies GetStaticPaths;

const page = Astro.props.page as Page<BlogPost>;
---

<BaseLayout lang="zh" title={`博客 第 ${page.currentPage} 页`} description="生活随笔、设计思考、旅行记录与产品评测。" switchTo="/en/blog">
  <section class="site-shell page-head">
    <p class="ui-font eyebrow">{page.total} 篇文章</p>
    <h1 class="ui-font">博客</h1>
  </section>
  <section class="site-shell section-space">
    <PostList posts={page.data} lang="zh" />
    <nav class="pager ui-font" aria-label="分页">
      {page.url.prev && <a href={page.url.prev}>上一页</a>}
      <span>{page.currentPage} / {page.lastPage}</span>
      {page.url.next && <a href={page.url.next}>下一页</a>}
    </nav>
  </section>
</BaseLayout>

<style>
  .page-head {
    padding-top: 88px;
  }

  .eyebrow {
    width: fit-content;
    margin: 0 0 12px;
    padding: 2px 12px;
    border: 2px solid var(--color-border);
    border-radius: 18px 12px 20px 13px / 12px 18px 10px 16px;
    background: var(--color-accent-2);
    box-shadow: var(--shadow-doodle-small);
    color: #fffdf3;
    font-size: 18px;
    font-weight: 700;
    transform: rotate(-2deg);
  }

  h1 {
    margin: 0;
    font-family: 'ZCOOL KuaiLe', 'Noto Serif SC', serif;
    font-size: 72px;
    font-weight: 400;
    letter-spacing: 0;
    text-shadow: 4px 4px 0 var(--color-surface-strong);
  }

  .pager {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 40px;
    color: var(--color-text);
    font-size: 18px;
    font-weight: 700;
  }
</style>
